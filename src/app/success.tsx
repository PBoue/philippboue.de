import { FC } from 'react'

interface successProps {}

const success: FC<successProps> = ({}) => {
    return (
        <div>Vielen Dank für die Kontaktanfrage! Ich melde mich so schnell wie möglich...</div>
    )
}

export default success